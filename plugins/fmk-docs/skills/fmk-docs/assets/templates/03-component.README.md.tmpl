---
type: aggregator
id: {{id}}
title: "{{title}}"
level: 3
parent: ../../README.md
status: fresh
last_synced: {{date}}
sha_method: git-blob-sha1
references: []
---

# Component: {{title}}

## Responsibility

What this component does within its container.

## Public surface

Exported types, public functions, HTTP routes, message handlers — whatever the rest of the system depends on.

## Code-level docs

Each significant code unit (file or logical group) is documented in `04-code/<name>.md`. Mirror those in the `references[]` list above as `kind: child` entries.

## Notes

Invariants, gotchas, design decisions worth flagging at the component level.
